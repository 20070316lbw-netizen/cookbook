# 出处和参考

## git / 模板出处
- `gitignore` 官方文档：https://git-scm.com/docs/gitignore
- GitHub 官方 `.gitignore` 模板集合（Python 段的来源）：
  https://github.com/github/gitignore  → `Python.gitignore`

## 背景
- 量化项目里需要忽略 `*.duckdb` / `.env` / `secrets.*` 等数据与敏感文件时，
  在通用 Python 模板基础上补出来的一份常用清单。本仓库根目录的 `.gitignore` 与此一致。

## 同 cookbook 内的相关条目
- `git/connect_my_git/` —— 账号配置与首次 commit
